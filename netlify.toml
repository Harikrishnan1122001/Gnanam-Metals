[build]
  command = "npm install --legacy-peer-deps && chmod +x ./node_modules/.bin/react-scripts && ./node_modules/.bin/react-scripts build"
  publish = "build"

[build.environment]
  CI = "false"
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"